// Created by 

#include "../include/polygon_maker.h"

std::vector<std::tuple<int, int, int>> MakePolygon(const PointSet& points) {
    // Make a big triangle.

    // Randomize points.

    // Repeat until the triangle is divided by all points.

    // Add triangle in edges.

    // Delete the big triangle.
}